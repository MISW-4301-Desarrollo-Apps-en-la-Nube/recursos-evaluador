name: Entrega 3 - Evaluador Implementación con Email
on:
  workflow_dispatch:
    inputs:
      email:
        description: 'Email'
        required: true
      environment:
        type: choice
        required: true
        description: 'Select the target environment'
        options:
          - student1
          - student2
          - student3
          - student4
          - tutor
  workflow_call:
    inputs:
      email:
        type: string
        description: 'Email'
        required: true
      environment:
        type: string
        required: true
        description: 'Select the target environment'
    secrets:
      AWS_ACCESS_KEY_ID:
        required: true
      AWS_SECRET_ACCESS_KEY:
        required: true
      AWS_SESSION_TOKEN:
        required: true
jobs:
  run-pipeline:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 18
          registry-url: https://registry.npmjs.org/
      - name: Create evaluator folder
        run: mkdir -p .evaluator
      - name: Download validate scripts
        run: curl -sSL -o .evaluator/entrega3.json https://raw.githubusercontent.com/MISW-4301-Desarrollo-Apps-en-la-Nube/proyecto-monitor/main/entrega3/entrega3.json
      - name: Checkout evaluator resources
        uses: actions/checkout@v4
        with:
          repository: MISW-4301-Desarrollo-Apps-en-la-Nube/recursos-evaluador
          ref: main
          path: .evaluator-resources
      - name: Connect to EKS cluster
        uses: ./.evaluator-resources/.github/actions/connect-eks-cluster
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
      - name: Test entrega 3 works
        run: newman run ".evaluator/entrega3.json" --env-var BASE_PATH=$BASE_PATH --env-var EMAIL=${{ inputs.email }} --verbose --insecure